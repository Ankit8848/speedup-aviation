{
  "name": "workspace",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "engines": {
    "node": "20.x"
  },
  "workspaces": [
    "artifacts/*",
    "packages/*",
    "scripts/*"
  ],
  "scripts": {
    "build": "npm run typecheck --workspaces",
    "typecheck": "tsc --build"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}